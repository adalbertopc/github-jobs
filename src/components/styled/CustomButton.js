import styled from 'styled-components';

export const CustomButton = styled.button`
	background: blue;
	color: #fff;
	font-size: 0.8rem;
	font-weight: 600;
	border: none;
	border-radius: 5px;
	cursor: pointer;
	padding: 15px 20px;
	outline: none;
	:hover {
		background: cyan;
	}
`;
